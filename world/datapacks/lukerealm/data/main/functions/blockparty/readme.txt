The rng math uses the ? differenciator unlike the regular scoreboards for blockParty which use #

fuuuuuuuuuuuck i just realized that doesn't work, switching it around now

I have to do another commit to enable the workflow